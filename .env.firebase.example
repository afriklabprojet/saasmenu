# Configuration Firebase pour RestroSaaS
# Copiez ces variables dans votre fichier .env et renseignez les valeurs

# Configuration principale Firebase
FIREBASE_ENABLED=true
FIREBASE_PROJECT_ID=your-firebase-project-id

# Clés d'authentification Firebase
FIREBASE_SERVER_KEY=your-firebase-server-key
FIREBASE_CREDENTIALS=path/to/firebase-credentials.json
FIREBASE_WEB_API_KEY=your-firebase-web-api-key

# Configuration Firebase Cloud Messaging
FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
FIREBASE_APP_ID=your-firebase-app-id
FIREBASE_MEASUREMENT_ID=your-measurement-id

# Configuration VAPID pour Web Push
FIREBASE_VAPID_PUBLIC_KEY=your-vapid-public-key
FIREBASE_VAPID_PRIVATE_KEY=your-vapid-private-key
FIREBASE_VAPID_SUBJECT=mailto:admin@yoursite.com

# Configuration des webhooks Firebase (optionnel)
FIREBASE_WEBHOOK_DELIVERY=
FIREBASE_WEBHOOK_OPEN=
FIREBASE_WEBHOOK_CLICK=
FIREBASE_WEBHOOK_UNSUBSCRIBE=

# Configuration Analytics Firebase
FIREBASE_ANALYTICS_ENABLED=true

# Configuration des logs Firebase
FIREBASE_LOGGING_ENABLED=true
FIREBASE_LOG_LEVEL=info
FIREBASE_LOG_CHANNEL=stack

# Configuration de la queue Firebase
FIREBASE_QUEUE_ENABLED=true
FIREBASE_QUEUE_CONNECTION=database
FIREBASE_QUEUE_NAME=firebase

# Configuration de test Firebase (développement)
FIREBASE_TESTING_ENABLED=false
FIREBASE_TEST_TOKEN=
FIREBASE_MOCK_RESPONSES=false

# Instructions pour obtenir les clés Firebase :
#
# 1. Allez sur https://console.firebase.google.com/
# 2. Créez un nouveau projet ou sélectionnez un projet existant
# 3. Allez dans Paramètres du projet > Général
# 4. Dans "Vos applications", cliquez sur l'icône web (</>)
# 5. Enregistrez votre application et copiez les valeurs de configuration
# 6. Pour les clés serveur, allez dans Cloud Messaging > Gérer les clés d'API
# 7. Pour VAPID, générez une paire de clés dans Cloud Messaging > Configuration Web
# 8. Pour le fichier credentials.json, allez dans Comptes de service > Générer une nouvelle clé privée
#
# Sécurité importante :
# - Ne jamais commiter le fichier credentials.json dans le contrôle de version
# - Utilisez des variables d'environnement sécurisées en production
# - Restreignez les clés API aux domaines/IPs autorisés
# - Activez la surveillance des quotas et alertes
